# BuyerForeSight Backend Assignment

## User Management REST API

This is a **Node.js + Express** backend project implementing a **User Management REST API** using **SQLite** as the database.  

The project supports **CRUD operations**, **search**, and **sorting** for users.

---

## **Project Structure**
